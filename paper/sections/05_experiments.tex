\section{Experiments}
\label{sec:experiments}

We evaluate ApproxDA-TransUNet on three medical image segmentation benchmarks spanning different global context requirements. The experiments systematically examine computational efficiency, task-dependent approximation behavior across GCR regimes, the dynamics of symmetric gate routing, and the relationship between approximation strength and task context.

\subsection{Experimental Setup}

\textbf{Datasets.} We evaluate ApproxDA on three representative medical image segmentation benchmarks covering different contextual characteristics.

\textit{Synapse Multi-Organ CT}~\cite{landman2015miccai}: 30 abdominal CT scans with an 18/12 train/test split covering 8 organ classes (9 classes with background). Evaluation metrics: mean DSC and HD95.

\textit{Kvasir-SEG}~\cite{jha2020kvasir}: 1000 endoscopy polyp images (binary segmentation) with an 800/200 train/test split. Evaluation metrics: DSC and mIoU.

\textit{ISIC 2018}~\cite{codella2019isic}: 2594 dermoscopy skin lesion images (binary segmentation) with a 2075/519 train/test split (80/20). Evaluation metrics: DSC and mIoU.

Table~\ref{tab:datasets} summarizes the three datasets alongside their contextual characteristics and GCR classification, which motivates the cross-task analysis in Section~\ref{sec:analysis}.

\begin{table}[t]
  \caption{Dataset Properties and Global Context Requirement (GCR)}
  \label{tab:datasets}
  \begin{center}
  \setlength{\tabcolsep}{3pt}
  \begin{tabular}{lcccc}
    \toprule
    Dataset & Classes & Object Type & Long-range Context & GCR \\
    \midrule
    Synapse    & 8 & Multi-organ CT  & High (inter-organ)   & High \\
    Kvasir-SEG & 1 & Polyp           & Low (local boundary) & Low  \\
    ISIC 2018  & 1 & Skin lesion     & Low (texture/edge)   & Low  \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vspace{-0.3em}
  \footnotesize{High-GCR tasks require cross-organ anatomical reasoning; low-GCR tasks are dominated by local appearance cues. GCR governs approximation effectiveness (Section~\ref{sec:analysis}).}
\end{table}

\textbf{Implementation Details.} All experiments use an R50-ViT-B/16 backbone pretrained on ImageNet-21K. Training uses SGD (momentum 0.9, weight decay $10^{-4}$), initial learning rate 0.01 with polynomial decay, batch size 24, image size $224{\times}224$, 300 epochs, and seed 1234. The DA-TransUNet baseline runs on T4${\times}$1. ApproxDA-TransUNet runs on T4${\times}$2 via \texttt{torchrun} DDP with per-GPU batch 12 and NCCL all-reduce, under identical total batch size and base learning rate. Default ApproxDA configuration: $M{=}7$, $r{=}32$, $G{=}8$. Our DA-TransUNet re-run achieves validation DSC 0.795, consistent with the paper-reported 0.798, confirming faithful replication.

\subsection{Computational Efficiency of ApproxDA}

Table~\ref{tab:efficiency} compares the computational characteristics of ApproxDA and DA-TransUNet on Synapse. Although per-GPU VRAM is reduced substantially---from 11.5\,GB to 6.4\,GB---total GFLOPs are slightly higher (32.1 vs.\ 30.2) because the shared ViT backbone dominates total computation and Conv1d/Linear projection overhead partially offsets decoder-level savings. The reduced memory footprint enables multi-GPU distributed training via DDP, which DA-TransUNet does not support.

\begin{table*}[t]
  \caption{Efficiency Comparison on Synapse (T4). GFLOPs measured by \texttt{fvcore} (single $224{\times}224$ slice, includes attention \texttt{bmm}).}
  \label{tab:efficiency}
  \begin{center}
  \setlength{\tabcolsep}{5pt}
  \begin{tabular}{lcccccc}
    \toprule
    Method & Params (M) & GFLOPs & Train VRAM & Train Time & Infer (s/vol) & Multi-GPU \\
    \midrule
    DA-TransUNet~\cite{sun2024datransunet} & 107.95 & 30.2 & 11.5\,GB & 11.4h $\times$1 & 120.0 & No$^\dagger$ \\
    \midrule
    ApproxDA (gate=pam, $M$=7, $r$=32)   & 112.98 & 32.1 & 6.4\,GB/GPU & 8.3h $\times$2 & 121.3 & Yes (DDP) \\
    ApproxDA (gate=learn, $M$=7, $r$=32) & 114.90 & 32.1 & 10.6\,GB & 11.5h $\times$1 & 114.2 & Yes (DDP) \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vspace{-0.3em}
  \footnotesize{$^\dagger$DA-TransUNet does not support multi-GPU training. ApproxDA uses DDP (\texttt{torchrun}, NCCL); per-GPU batch 12 (total 24), LR unchanged.}
\end{table*}

\subsection{Task-dependent Behavior of Attention Approximation}

We examine whether efficient attention approximation behaves consistently across medical segmentation tasks with different contextual requirements. Tables~\ref{tab:synapse}--\ref{tab:isic} compare ApproxDA-TransUNet against DA-TransUNet and established baselines on all three benchmarks.

\textbf{Synapse.} Table~\ref{tab:synapse} compares DSC and HD95 against 11 published methods. CNN-based methods achieve 76--78\% DSC. Transformer-based methods progressively improve global context modeling, with Swin-Unet (79.13\%) and DA-TransUNet (79.80\%) previously topping the leaderboard. ApproxDA-TransUNet (gate=pam, $M{=}28$, $r{=}32$) achieves \textbf{80.94\%} DSC and 27.49\,mm HD95---surpassing all baselines including DA-TransUNet by $+$1.14\%. Window sensitivity ablation (Section~\ref{sec:rq2}) reveals a non-monotonic DSC-vs-$M$ curve peaking at $M{=}28$: both too-local ($M{=}7$: 78.64\%) and fully-global ($M{=}112$: 79.44\%) attention underperform the intermediate optimum.

\begin{table}[t]
  \caption{Segmentation Results on Synapse Multi-Organ CT}
  \label{tab:synapse}
  \begin{center}
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{lcc}
    \toprule
    Method & DSC (\%) $\uparrow$ & HD95 (mm) $\downarrow$ \\
    \midrule
    \multicolumn{3}{l}{\textit{CNN-based}} \\
    U-Net~\cite{ronneberger2015unet}              & 76.85 & 39.70 \\
    U-Net++~\cite{zhou2018unetpp}                 & 76.91 & 36.93 \\
    Residual U-Net~\cite{diakogiannis2020resunet} & 76.95 & 38.44 \\
    Att-Unet~\cite{oktay2018attention}            & 77.77 & 36.02 \\
    MultiResUNet~\cite{ibtehaz2020multiresunet}   & 77.42 & 36.84 \\
    \midrule
    \multicolumn{3}{l}{\textit{Transformer-based}} \\
    TransUNet~\cite{chen2021transunet}            & 77.48 & 31.69 \\
    UCTransNet~\cite{wang2022uctransnet}          & 78.23 & 26.75 \\
    TransNorm~\cite{azad2022transnorm}            & 78.40 & 30.25 \\
    MIM~\cite{wang2022mim}                        & 78.59 & \underline{26.59} \\
    Swin-Unet~\cite{cao2022swinunet}              & 79.13 & 21.55 \\
    DA-TransUNet~\cite{sun2024datransunet}        & \textbf{79.80} & \textbf{23.48} \\
    \midrule
    \multicolumn{3}{l}{\textit{Dual-attention approximation (ours)}} \\
    ApproxDA-TransUNet                            & \underline{80.94} & \underline{27.49} \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vspace{-0.3em}
  \footnotesize{Baselines from~\cite{sun2024datransunet} Table~1 (SGD, 500ep, 224$\times$224). ApproxDA: gate=pam, $M$=28, $r$=32, $G$=8, SGD 300ep (window sensitivity ablation identifies $M{=}28$ as optimal). We also re-ran DA-TransUNet under our protocol (SGD, 300ep) and obtained 72.03\% DSC; the gap from paper-reported (79.80\%) is primarily due to optimizer and epoch differences (Adam/500ep vs.\ SGD/300ep), so we report paper-reported values for all Synapse baselines.}
\end{table}

\textbf{Kvasir-SEG.} Table~\ref{tab:kvasir} compares polyp segmentation results against all six methods reported in~\cite{sun2024datransunet}. We re-ran DA-TransUNet under the same SGD/300ep protocol as ApproxDA to enable a direct comparison, obtaining 88.44\% DSC, 81.70\% mIoU, and 53.04\,mm HD95. ApproxDA-TransUNet (gate=pam, $M$=56) achieves 90.17\% DSC, 84.27\% mIoU, and 44.35\,mm HD95---outperforming the reproduced DA-TransUNet baseline (+1.73\% DSC, +2.57\% mIoU, $-$8.69\,mm HD95).

\begin{table}[t]
  \caption{Segmentation Results on Kvasir-SEG Polyp}
  \label{tab:kvasir}
  \begin{center}
  \setlength{\tabcolsep}{3pt}
  \begin{tabular}{lccc}
    \toprule
    Method & DSC (\%) $\uparrow$ & mIoU (\%) $\uparrow$ & HD95 (mm) $\downarrow$ \\
    \midrule
    U-Net~\cite{ronneberger2015unet}           & 88.22 & 80.12 & -- \\
    Att-Unet~\cite{oktay2018attention}         & 86.61 & 78.01 & -- \\
    U-Net++~\cite{zhou2018unetpp}              & 86.57 & 77.67 & -- \\
    Res-UNet~\cite{diakogiannis2020resunet}    & 77.85 & 66.04 & -- \\
    TransUNet~\cite{chen2021transunet}         & 87.91 & 80.03 & -- \\
    DA-TransUNet~\cite{sun2024datransunet}$^\dag$ & \underline{88.44} & \underline{81.70} & \underline{53.04} \\
    \midrule
    \textbf{ApproxDA-TransUNet (ours)}         & \textbf{90.17} & \textbf{84.27} & \textbf{44.35} \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vspace{-0.3em}
  \footnotesize{Other baselines from~\cite{sun2024datransunet} Table~2 (Adam, 500ep, 75/25 split); HD95 not reported. $^\dag$DA-TransUNet re-run under the same protocol as ApproxDA (SGD, 300ep, 80/20 split). Paper-reported~\cite{sun2024datransunet}: 88.47\% DSC, 81.02\% mIoU. ApproxDA: gate=pam, $M$=56, $r$=32.}
\end{table}

\textbf{ISIC 2018.} Table~\ref{tab:isic} reports skin lesion segmentation results against the same six baselines from~\cite{sun2024datransunet}. Our ApproxDA experimental results are pending completion.

\begin{table}[t]
  \caption{Segmentation Results on ISIC 2018 Skin Lesion}
  \label{tab:isic}
  \begin{center}
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{lcc}
    \toprule
    Method & DSC (\%) $\uparrow$ & mIoU (\%) $\uparrow$ \\
    \midrule
    U-Net~\cite{ronneberger2015unet}           & 87.22 & 81.14 \\
    Att-Unet~\cite{oktay2018attention}         & 87.60 & 81.51 \\
    U-Net++~\cite{zhou2018unetpp}              & 87.30 & 81.33 \\
    Res-UNet~\cite{diakogiannis2020resunet}    & 83.32 & 76.51 \\
    TransUNet~\cite{chen2021transunet}         & \underline{88.78} & \underline{82.63} \\
    DA-TransUNet~\cite{sun2024datransunet}     & \textbf{88.88} & \textbf{82.78} \\
    \midrule
    \textbf{ApproxDA-TransUNet (ours)}         & \textit{pend.} & \textit{pend.} \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vspace{-0.3em}
  \footnotesize{Baselines from~\cite{sun2024datransunet} Table~2 (Adam, 50ep, 75/25 split). ApproxDA: SGD, 300ep, 80/20 split.}
\end{table}

With appropriate window selection, ApproxDA outperforms DA-TransUNet on both datasets: $+$1.14\% DSC on Synapse ($M{=}28$) and $+$1.73\% DSC on Kvasir ($M{=}56$). However, the sensitivity to window choice differs markedly: Synapse DSC spans 2.30\% across $M$ values while Kvasir spans only 0.64\%, a 3.6$\times$ ratio reflecting the different global context requirements of the two tasks.

\subsection{Analysis of Symmetric Gate Collapse}

Although the learnable gate (\texttt{gate=learn}) was designed to dynamically balance PAM and CAM branches, Table~\ref{tab:gate_ablation} reveals that it consistently ranks last on Synapse. The gate coefficient converges to $g{\approx}0.5$ and remains nearly constant throughout training, producing a fixed 50/50 blend that performs below both PAM-only (78.64\%) and CAM-only (78.26\%) configurations. This collapse is observed across both $M{=}7$ and $M{=}14$, confirming that it is a property of symmetric two-branch optimization rather than a consequence of limited receptive field. The gradient-level derivation is provided in Section~\ref{sec:collapse}.

\begin{table}[t]
  \caption{Gate Mode Ablation on Synapse ($M$=7, $r$=32 unless noted)}
  \label{tab:gate_ablation}
  \begin{center}
  \setlength{\tabcolsep}{4pt}
  \small
  \begin{tabular}{llcc}
    \toprule
    Gate Mode & $g$ & DSC (\%) $\uparrow$ & HD95 (mm) $\downarrow$ \\
    \midrule
    gate=pam                    & 1.0              & \textbf{78.64} & 31.09 \\
    gate=cam                    & 0.0              & \underline{78.26}  & \underline{30.59} \\
    gate=learn ($M$=14, $r$=64) & ${\approx}0.5^*$ & 78.04          & \textbf{29.09} \\
    gate=learn                  & ${\approx}0.5^*$ & 77.78          & 34.29 \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vspace{-0.3em}
  \footnotesize{All rows: ApproxDA-TransUNet. $^*$Gate collapsed to $g{\approx}0.5$ throughout training; see Section~\ref{sec:collapse}. \textbf{Bold}: best; \underline{underline}: second best per column.}
\end{table}

\subsection{GCR and Approximation Effectiveness}

\textbf{Which approximation axis drives the performance gap?} Table~\ref{tab:window_ablation} isolates the contribution of spatial windowing by comparing windowed ($M{=}7$) against effectively global ($M{=}112$, window-clamped) attention under gate=pam. The $M{=}14$ result from the gate ablation (gate=learn) is also shown for reference to provide a denser sampling of window sizes.

\begin{table}[t]
  \caption{Spatial Approximation Ablation on Synapse ($G$=8)}
  \label{tab:window_ablation}
  \begin{center}
  \setlength{\tabcolsep}{2pt}
  \small
  \begin{tabular}{lccccc}
    \toprule
    Config & $M$ & $r$ & Gate & DSC (\%) $\uparrow$ & HD95 (mm) $\downarrow$ \\
    \midrule
    Windowed + low-rank & 7   & 32 & pam          & 78.64          & 31.09 \\
    Windowed + low-rank & 14  & 64 & learn$^\dag$ & 78.04          & 29.09 \\
    Windowed + low-rank & 28  & 32 & pam          & \textbf{80.94} & \underline{27.49} \\
    Global + low-rank   & 112 & 32 & pam          & \underline{79.44} & \textbf{27.26} \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vspace{-0.3em}
  \footnotesize{$^\dag$Gate collapsed to $g{\approx}0.5$; gate mode differs from the other rows. $M{=}112$ via window clamping yields full-map attention at all scales. The DSC-vs-$M$ curve is \emph{non-monotonic}: peak at $M{=}28$ (80.94\%, $+$1.14\% vs.\ DA-TransUNet), with both $M{=}7$ (78.64\%) and $M{=}112$ (79.44\%) underperforming. DSC range across $M$ = 2.30\%---3.6$\times$ Kvasir's 0.64\% sensitivity range, operationalizing GCR as a predictor of window sensitivity.}
\end{table}

The Phase~D Synapse window sensitivity curve (gate=pam, $r{=}32$, $M{\in}\{7,28,112\}$) is \emph{non-monotonic}: DSC peaks at $M{=}28$ (80.94\%, $+$1.14\% vs.\ DA-TransUNet), with fully-global attention ($M{=}112$: 79.44\%) underperforming the intermediate optimum by 1.50\%. The curve spans 2.30\% across the three $M$ values, confirming high window sensitivity on this high-GCR task. In contrast, the Phase~D Kvasir window sensitivity curve ($M{\in}\{7,28,56,112\}$) spans only 0.64\% DSC---confirming low GCR: Kvasir performance is nearly window-size-invariant. The sensitivity ratio (2.30\% vs.\ 0.64\%, ${\approx}3.6{\times}$) operationalizes GCR without requiring a formal metric (Table~\ref{tab:cross_task}).

\textbf{Cross-task summary.} Table~\ref{tab:cross_task} consolidates results across all benchmarks. With task-appropriate window selection, ApproxDA outperforms DA-TransUNet on both benchmarks: $+$1.14\% DSC on high-GCR Synapse ($M{=}28$) and $+$1.73\% DSC on low-GCR Kvasir-SEG ($M{=}56$). However, the performance sensitivity to window choice differs markedly: Synapse spans 2.30\% DSC across $M$ values while Kvasir spans only 0.64\%---a 3.6$\times$ sensitivity ratio that operationalizes GCR as a predictor of window importance. High-GCR tasks require careful window tuning to achieve gains; low-GCR tasks are window-robust. Figure~\ref{fig:qualitative} illustrates this context-dependent pattern qualitatively.

\begin{table}[t]
  \caption{Cross-Task Approximation Effectiveness by GCR Level}
  \label{tab:cross_task}
  \begin{center}
  \setlength{\tabcolsep}{3pt}
  \small
  \begin{tabular}{lcccc}
    \toprule
    Dataset & GCR & DA-TransUNet$^\dagger$ & Best ApproxDA & $\Delta$ DSC \\
    \midrule
    Synapse    & High & 79.80\% & 80.94\%$^a$ & $+$1.14\% \\
    Kvasir-SEG & Low  & 88.44\%$^c$ & 90.17\%$^b$ & $+$1.73\% \\
    ISIC 2018  & Low  & 88.88\% & \textit{pend.} & \textit{pend.} \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vspace{-0.3em}
  \footnotesize{$^\dagger$Paper-reported~\cite{sun2024datransunet} except Kvasir-SEG which is our re-run (SGD, 300ep, 80/20 split). $^a$gate=pam, $M$=28, $r$=32 (window ablation peak). $^b$gate=pam, $M$=56, $r$=32 (window ablation peak). $^c$Our re-run; paper-reported: 88.47\%.}
\end{table}

Together, the controlled experiments across datasets, gate modes, and window sizes establish a consistent picture: attention approximation behaves differently depending on the global context requirements of the target task. These observations motivate the formal GCR analysis in Section~\ref{sec:analysis}.

\begin{figure}[t]
  \centering
  \fbox{\rule{0pt}{4cm}\rule{\columnwidth}{0pt}}
  \caption{Qualitative segmentation comparison. \textbf{Top (Synapse):} ApproxDA ($M{=}28$) matches or exceeds DA-TransUNet on multi-organ boundaries, though performance is sensitive to window size (range 2.30\% across $M$ values). \textbf{Bottom (Kvasir-SEG):} ApproxDA ($M{=}56$) matches or exceeds DA-TransUNet on polyp boundaries; performance is window-robust (range 0.64\%). [Generated after experiments complete.]}
  \label{fig:qualitative}
\end{figure}
