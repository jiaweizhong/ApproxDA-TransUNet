% !TEX root = ../ApproxDA-TransUNet.tex
\section{Results}
\label{sec:experiments}

We evaluate ApproxDA-TransUNet on five benchmarks spanning tasks with fundamentally different spatial context requirements, comparing against DA-TransUNet and other competitive baselines.

\subsection{Experimental Setup}

\textbf{Datasets.} We evaluate on five representative medical image segmentation benchmarks: Synapse multi-organ CT~\cite{landman2015miccai} (8 organ classes; mean DSC and HD95), ACDC cardiac MRI~\cite{bernard2018deep} (3 cardiac structures; DSC and HD95), Kvasir-SEG~\cite{jha2020kvasir} (1,000 endoscopy polyp images; DSC and mIoU), CVC-ClinicDB~\cite{bernal2015cvc} (612 colonoscopy polyp images; DSC, mIoU, and HD95), and ISIC 2018~\cite{codella2019isic} (2,594 dermoscopy skin lesion images; DSC and mIoU).

\textbf{Implementation Details.} All experiments use an R50-ViT-B/16 backbone pretrained on ImageNet-21K, trained on NVIDIA Tesla T4 (16\,GB) GPUs. Optimizer: SGD, lr$=$0.01; batch size 24; input $224{\times}224$; 300 epochs; best checkpoint selected by evaluating every 15 epochs. Default ApproxDA-TransUNet configuration: $M{=}7$, $r{=}32$, $G{=}8$.

\textbf{Gate Protocol.} ApproxDA-TransUNet supports four gate modes: \texttt{pam} ($g{=}1.0$, PAM only), \texttt{cam} ($g{=}0.0$, CAM only), \texttt{fixed} ($g{=}0.5$, equal blend), and \texttt{learn} ($g$ learned per channel). Each experiment's gate configuration is specified in the corresponding table footnote.

\subsection{Comparison with State-of-the-Art}

Tables~\ref{tab:synapse}--\ref{tab:isic} compare ApproxDA-TransUNet against published baselines across all five benchmarks.

\textbf{Synapse.} Table~\ref{tab:synapse} benchmarks against 11 published methods. CNN-based models plateau at 76--78\% DSC, with Att-UNet leading at 77.77\%. Transformer-based methods progressively improve: TransUNet (77.48\%), UCTransNet (78.23\%), MIM (78.59\%), Swin-Unet (79.13\%), and DA-TransUNet (79.80\%). ApproxDA-TransUNet with gate=pam, $M{=}28$, $r{=}32$ achieves \textbf{80.94\%} DSC and 27.49\,mm HD95---the highest DSC across all compared methods, surpassing DA-TransUNet by $+$1.14\%. The intermediate window $M{=}28$ captures sufficient cross-organ context while limiting spurious long-range correlations; Section~\ref{sec:understanding} analyzes this behavior in detail.

\begin{table}[ht]
  \caption{Segmentation Results on Synapse Multi-Organ CT}
  \label{tab:synapse}
  \centering
  \renewcommand{\arraystretch}{1.15}
  \setlength{\tabcolsep}{6pt}
  \begin{tabular}{lcc}
    \toprule
    Method & DSC (\%) $\uparrow$ & HD95 (mm) $\downarrow$ \\
    \midrule
    \multicolumn{3}{l}{\textit{CNN-based}} \\
    U-Net~\cite{ronneberger2015unet}              & 76.85 & 39.70 \\
    U-Net++~\cite{zhou2018unetpp}                 & 76.91 & 36.93 \\
    Residual U-Net~\cite{diakogiannis2020resunet} & 76.95 & 38.44 \\
    MultiResUNet~\cite{ibtehaz2020multiresunet}   & 77.42 & 36.84 \\
    Att-Unet~\cite{oktay2018attention}            & 77.77 & 36.02 \\
    \midrule
    \multicolumn{3}{l}{\textit{Transformer-based}} \\
    TransUNet~\cite{chen2021transunet}            & 77.48 & 31.69 \\
    UCTransNet~\cite{wang2022uctransnet}          & 78.23 & 26.75 \\
    TransNorm~\cite{azad2022transnorm}            & 78.40 & 30.25 \\
    MIM~\cite{wang2022mim}                        & 78.59 & 26.59 \\
    Swin-Unet~\cite{cao2022swinunet}              & 79.13 & \textbf{21.55} \\
    DA-TransUNet~\cite{sun2024datransunet}        & \underline{79.80} & \underline{23.48} \\
    \midrule
    \multicolumn{3}{l}{\textit{Dual-attention approximation (ours)}} \\
    \textbf{ApproxDA-TransUNet (ours)}     & \textbf{80.94} & 27.49 \\
    \bottomrule
  \end{tabular}
  \par\vspace{5pt}
  \parbox{\columnwidth}{\footnotesize ApproxDA: gate=pam, $M{=}28$, $r{=}32$, $G{=}8$.}
\end{table}

\textbf{ACDC.} On cardiac MRI segmentation (Table~\ref{tab:acdc}), DA-TransUNet achieves a mean DSC of 88.50\% (RV 88.84\%, Myo \textbf{85.98\%}, LV 90.67\%). ApproxDA-TransUNet with gate=pam, $M{=}7$ achieves a mean DSC of 88.97\% ($+$0.47\% over DA-TransUNet), attaining the \textbf{highest} per-class DSC on RV (\textbf{89.61\%}) and competitive performance on Myo (\underline{85.91\%}). While both dual-attention architectures exhibit lower LV cavity DSC than pure global Transformer models (e.g., TransUNet 95.73\%, Swin-Unet 95.83\%), ApproxDA outperforms DA-TransUNet across both RV ($+$0.77\%) and LV ($+$0.73\%). The class-level pattern is consistent with the inductive-bias interpretation: local windowed attention benefits locally-resolvable structures whose boundaries are determined by fine-grained intensity gradients (crescent-shaped RV; thin-ring Myo), while LV cavity delineation requires estimating global cavity extent spanning multiple $M{=}7$ windows. Within a nominally low-GCS dataset ($\Delta\text{DSC}{=}0.73$\,pp overall), ApproxDA maintains a consistent advantage over full-attention DA-TransUNet.

\begin{table}[ht]
  \caption{Segmentation Results on ACDC Cardiac MRI}
  \label{tab:acdc}
  \centering
  \renewcommand{\arraystretch}{1.15}
  \setlength{\tabcolsep}{3pt}
  \small
  \begin{tabular}{lcccc}
    \toprule
    Method & RV & Myo & LV & Mean (\%) $\uparrow$ \\
    \midrule
    U-Net~\cite{ronneberger2015unet}$^\dag$          & 87.10 & 80.63 & 94.92 & 87.55 \\
    Att-UNet~\cite{oktay2018attention}$^\dag$         & 87.58 & 79.20 & 93.47 & 86.75 \\
    TransUNet~\cite{chen2021transunet}                & \underline{88.86} & 84.53 & \underline{95.73} & \underline{89.71} \\
    Swin-Unet~\cite{cao2022swinunet}                  & 88.55 & 85.62 & \textbf{95.83} & \textbf{90.00} \\
    DA-TransUNet~\cite{sun2024datransunet}$^\ddag$    & 88.84 & \textbf{85.98} & 90.67 & 88.50 \\
    \midrule
    \textbf{ApproxDA-TransUNet (ours)} & \textbf{89.61} & \underline{85.91} & 91.40 & 88.97 \\
    \bottomrule
  \end{tabular}
  \par\vspace{5pt}
  \parbox{\columnwidth}{\footnotesize $^\dag$R50-backbone variant reported in~\cite{chen2021transunet}. $^\ddag$Re-run under our protocol. ApproxDA: gate=pam, $M{=}7$, $r{=}32$, $G{=}8$.}
\end{table}

\textbf{Kvasir-SEG.} On polyp segmentation (Table~\ref{tab:kvasir}), U-Net leads the CNN-based group (88.22\% DSC) while TransUNet (87.91\%) and DA-TransUNet$^\dag$ (88.44\%) are the strongest Transformer-based entries. ApproxDA-TransUNet with gate=pam, $M{=}56$ achieves \textbf{90.17\%} DSC, \textbf{84.27\%} mIoU, and \textbf{44.35\,mm} HD95---state-of-the-art across all three metrics, with a $+$1.73\% DSC gain and $-$8.69\,mm HD95 improvement over the nearest baseline.

\begin{table}[ht]
  \caption{Segmentation Results on Kvasir-SEG Polyp}
  \label{tab:kvasir}
  \centering
  \setlength{\tabcolsep}{3pt}
  \begin{tabular}{lccc}
    \toprule
    Method & DSC (\%) $\uparrow$ & mIoU (\%) $\uparrow$ & HD95 (mm) $\downarrow$ \\
    \midrule
    U-Net~\cite{ronneberger2015unet}           & 88.22 & 80.12 & -- \\
    Att-Unet~\cite{oktay2018attention}         & 86.61 & 78.01 & -- \\
    U-Net++~\cite{zhou2018unetpp}              & 86.57 & 77.67 & -- \\
    Res-UNet~\cite{diakogiannis2020resunet}    & 77.85 & 66.04 & -- \\
    TransUNet~\cite{chen2021transunet}         & 87.91 & 80.03 & -- \\
    DA-TransUNet~\cite{sun2024datransunet}$^\dag$ & \underline{88.44} & \underline{81.70} & \underline{53.04} \\
    \midrule
    \textbf{ApproxDA-TransUNet (ours)}         & \textbf{90.17} & \textbf{84.27} & \textbf{44.35} \\
    \bottomrule
  \end{tabular}
  \par\vspace{5pt}
  \parbox{\columnwidth}{\footnotesize $^\dag$Re-run under our protocol (88.47\% reported). ApproxDA: gate=pam, $M{=}56$, $r{=}32$.}
\end{table}

\textbf{CVC-ClinicDB.} On colonoscopy polyp segmentation (Table~\ref{tab:cvc}), DA-TransUNet achieves 89.47\% DSC and 82.51\% mIoU---the strongest published baseline on this dataset. ApproxDA-TransUNet with gate=pam, $M{=}112$ achieves \textbf{90.99\%} DSC, \textbf{85.09\%} mIoU, and \textbf{14.77\,mm} HD95---surpassing the nearest baseline by $+$1.52\% DSC and $+$2.58\% mIoU. The DSC span across $M{\in}\{7,28,56,112\}$ is 0.62\,pp, confirming CVC-ClinicDB is window-robust---consistent with its binary task structure and low GCS, as analyzed in Section~\ref{sec:understanding}.

\begin{table}[ht]
  \caption{Segmentation Results on CVC-ClinicDB Polyp}
  \label{tab:cvc}
  \centering
  \setlength{\tabcolsep}{3pt}
  \begin{tabular}{lccc}
    \toprule
    Method & DSC (\%) $\uparrow$ & mIoU (\%) $\uparrow$ & HD95 (mm) $\downarrow$ \\
    \midrule
    U-Net~\cite{ronneberger2015unet}                 & 86.93 & 78.21 & -- \\
    Att-Unet~\cite{oktay2018attention}               & 87.41 & 79.35 & -- \\
    U-Net++~\cite{zhou2018unetpp}                    & 87.14 & 78.47 & -- \\
    Res-UNet~\cite{diakogiannis2020resunet}          & 74.22 & 59.02 & -- \\
    TransUNet~\cite{chen2021transunet}               & 89.01 & 81.63 & -- \\
    DA-TransUNet~\cite{sun2024datransunet}           & \underline{89.47} & \underline{82.51} & -- \\
    \midrule
    \textbf{ApproxDA-TransUNet (ours)}               & \textbf{90.99} & \textbf{85.09} & \textbf{14.77} \\
    \bottomrule
  \end{tabular}
  \par\vspace{5pt}
  \parbox{\columnwidth}{\footnotesize ApproxDA: gate=pam, $M{=}112$, $r{=}32$.}
\end{table}

\textbf{ISIC 2018.} On skin lesion segmentation (Table~\ref{tab:isic}), CNN-based methods cluster between 83--88\% DSC, with TransUNet (88.78\%) and DA-TransUNet (88.88\%) leading among Transformer-based approaches. ApproxDA-TransUNet with gate=learn, $M{=}7$ achieves \textbf{89.58\%} DSC ($+$0.70\% over DA-TransUNet) and 82.66\% mIoU, attaining the best DSC across all compared methods. This window-robust pattern is consistent with the low-GCS behavior observed on Kvasir-SEG (GCS defined in Section~\ref{sec:obs2}).

\begin{table}[ht]
  \caption{Segmentation Results on ISIC 2018 Skin Lesion}
  \label{tab:isic}
  \centering
  \renewcommand{\arraystretch}{1.15}
  \setlength{\tabcolsep}{6pt}
  \begin{tabular}{lcc}
    \toprule
    Method & DSC (\%) $\uparrow$ & mIoU (\%) $\uparrow$ \\
    \midrule
    U-Net~\cite{ronneberger2015unet}           & 87.22 & 81.14 \\
    Att-Unet~\cite{oktay2018attention}         & 87.60 & 81.51 \\
    U-Net++~\cite{zhou2018unetpp}              & 87.30 & 81.33 \\
    Res-UNet~\cite{diakogiannis2020resunet}    & 83.32 & 76.51 \\
    TransUNet~\cite{chen2021transunet}         & 88.78 & 82.63 \\
    DA-TransUNet~\cite{sun2024datransunet}     & \underline{88.88} & \textbf{82.78} \\
    \midrule
    \textbf{ApproxDA-TransUNet (ours)}         & \textbf{89.58} & \underline{82.66} \\
    \bottomrule
  \end{tabular}
  \par\vspace{5pt}
  \parbox{\columnwidth}{\footnotesize ApproxDA: gate=learn, $M{=}7$, $r{=}32$, $G{=}8$.}
\end{table}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/fig_qualitative_cross_task.png}
  \caption{Qualitative segmentation comparisons across representative benchmarks: Synapse multi-organ CT ($M{=}28$, top), Kvasir-SEG polyp ($M{=}56$, middle), and ISIC 2018 skin lesion ($M{=}7$, bottom). ApproxDA-TransUNet produces sharper organ/lesion contours while effectively suppressing false-positive boundary leakage.}
  \label{fig:qualitative}
\end{figure*}

\textbf{Qualitative Analysis.} Across five benchmarks (Figure~\ref{fig:qualitative}), ApproxDA-TransUNet achieves state-of-the-art or competitive results. The qualitative comparisons illustrate clear behavioral distinctions among the evaluated architectures:
\begin{itemize}
  \item \textbf{U-Net}~\cite{ronneberger2015unet}, as a pure CNN baseline lacking long-range dependency modeling, often produces boundary leakage into ambiguous background regions (e.g., lower-left false positive in Kvasir-SEG) and irregular, jagged contours on skin lesions in ISIC 2018.
  \item \textbf{TransUNet}~\cite{chen2021transunet} mitigates false-positive detections by incorporating ViT self-attention into the bottleneck to encode global spatial context. However, lacking fine-grained spatial-channel dual attention or localized boundary refinement, its predictions tend to be overly smooth, missing subtle lesion margins.
  \item \textbf{DA-TransUNet}~\cite{sun2024datransunet} captures dense spatial and channel dependencies via full PAM and CAM, improving upon TransUNet. Nonetheless, unconstrained global spatial attention can introduce subtle boundary blurriness on low-GCS tasks where distant pixel interactions add non-informative contextual noise.
  \item \textbf{ApproxDA-TransUNet (ours)} delivers superior boundary conformity and suppresses spurious background artifacts across datasets (achieving slice DSCs of 0.971 on Kvasir-SEG and 0.975 on ISIC 2018). The restricted spatial attention window preserves fine-grained texture priors, providing the ideal inductive bias for biomedical boundaries.
\end{itemize}


\subsection{Efficiency Analysis}

Although the proposed approximation substantially reduces the theoretical complexity of the dual-attention module, the overall computational cost is dominated by the ViT backbone and convolutional encoder, which remain unchanged. As shown in Table~\ref{tab:efficiency}, end-to-end GFLOPs and memory exhibit only marginal differences from DA-TransUNet. The practical advantage of ApproxDA-TransUNet therefore lies in improved architectural compatibility rather than total model complexity reduction: DA-TransUNet does not support DDP training, while ApproxDA-TransUNet trains natively across multiple GPUs, enabling cost-effective distributed training on commodity clusters.

\begin{table*}[ht]
  \caption{Efficiency Comparison on Synapse (NVIDIA Tesla T4)}
  \label{tab:efficiency}
  \centering
  \setlength{\tabcolsep}{5pt}
  \begin{tabular}{lcccccc}
    \toprule
    Method & Params (M) & GFLOPs & Train VRAM & Train Time & Infer (s/vol) & Multi-GPU \\
    \midrule
    DA-TransUNet~\cite{sun2024datransunet} & 107.95 & 30.2 & 11.5\,G & 11.4h & 120.0 & No$^\dagger$ \\
    \midrule
    ApproxDA-TransUNet (gate=pam, $M$=7)  & 112.98 & 32.1 & 6.4\,G ($\times$2 GPU) & 8.3h ($\times$2 GPU) & 121.3 & Yes (DDP) \\
    ApproxDA-TransUNet (gate=learn, $M$=7) & 114.90 & 32.1 & 10.6\,G & 11.5h & 114.2 & Yes (DDP) \\
    \bottomrule
  \end{tabular}
  \par\vspace{5pt}
  \parbox{\textwidth}{\footnotesize $^\dagger$DA-TransUNet produces non-contiguous gradients during backpropagation under PyTorch DDP, preventing multi-GPU distribution.}
\end{table*}

\subsection{Summary and Transition to Mechanistic Analysis}

The quantitative and qualitative evaluations across five heterogeneous benchmarks highlight two core empirical observations:
\begin{enumerate}
  \item \textbf{Approximation consistently outperforms full dual attention across benchmarks:} Across all five benchmarks, ApproxDA-TransUNet surpasses DA-TransUNet ($+$1.14\% on Synapse, $+$0.47\% on ACDC, $+$1.73\% on Kvasir-SEG, $+$1.52\% on CVC-ClinicDB, and $+$0.70\% on ISIC 2018), demonstrating that unconstrained global spatial interactions are not necessary and can introduce non-informative contextual noise.
  \item \textbf{Spatial context sensitivity is intrinsically task-dependent:} While multi-organ CT (Synapse) demands careful window tuning to balance local detail and cross-organ context, cardiac, polyp, and skin lesion tasks exhibit broad performance plateaus.
\end{enumerate}
These findings challenge the conventional view of attention approximation as a mere efficiency compromise and motivate two fundamental questions: \emph{What intrinsic task properties dictate this context sensitivity (investigated in Section~\ref{sec:gcs_mechanism})?} and \emph{How does spatial approximation alter training dynamics and architectural routing (investigated in Section~\ref{sec:optimization_routing})?}

